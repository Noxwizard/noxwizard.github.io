<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>crackme on Noxwizard's Blog</title><link>https://noxwizard.github.io/tags/crackme/</link><description>Recent content in crackme on Noxwizard's Blog</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><copyright>© Patrick Webster 2021</copyright><lastBuildDate>Fri, 21 May 2021 14:43:00 -0600</lastBuildDate><atom:link href="https://noxwizard.github.io/tags/crackme/index.xml" rel="self" type="application/rss+xml"/><item><title>ESET CrackMe 2017</title><link>https://noxwizard.github.io/posts/eset-crackme-2017/</link><pubDate>Fri, 21 May 2021 14:43:00 -0600</pubDate><guid>https://noxwizard.github.io/posts/eset-crackme-2017/</guid><description>ESET CrackMe 2017 Walkthrough The antivirus company, ESET, publishes “crackme”s as part of their application process for hiring reverse engineers. The crackme.zip in this discussion came from May 2021 and has a MD5 hash of 429db63eb6937538f54dd94808a454df. Based on the date of the signatures on the file, this was initially published in 2017. This article is the documentation of my attempt at solving the challenge. It may contain rabbit holes, inaccurate initial assumptions, or other problems.</description><content>&lt;h1 id="eset-crackme-2017-walkthrough">ESET CrackMe 2017 Walkthrough&lt;/h1>
&lt;p>The antivirus company, &lt;a href="https://www.eset.com/">ESET&lt;/a>, &lt;a href="https://join.eset.com/en/challenges/crack-me">publishes&lt;/a> “crackme”s as part of their application process for hiring reverse engineers. The &lt;a href="../../files/rebuilding-pe-files/crackme.zip">crackme.zip&lt;/a> in this discussion came from May 2021 and has a MD5 hash of &lt;code>429db63eb6937538f54dd94808a454df&lt;/code>. Based on the date of the signatures on the file, this was initially published in 2017. This article is the documentation of my attempt at solving the challenge. It may contain rabbit holes, inaccurate initial assumptions, or other problems. Some of this may be cleaned up at a later date.&lt;/p>
&lt;p>If your intent is to apply to ESET, please don&amp;rsquo;t use this post to solve it. That would be a disservice to yourself and ESET.&lt;/p>
&lt;p>If opening the file in 7zip, you will see the following:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>Name&lt;/th>
&lt;th>Size&lt;/th>
&lt;th>Virtual Size&lt;/th>
&lt;th>Offset&lt;/th>
&lt;th>Virtual Address&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>.data&lt;/td>
&lt;td>4608&lt;/td>
&lt;td>11960&lt;/td>
&lt;td>90112&lt;/td>
&lt;td>0x18000&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>.rdata&lt;/td>
&lt;td>22528&lt;/td>
&lt;td>22348&lt;/td>
&lt;td>67584&lt;/td>
&lt;td>0x12000&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>.text&lt;/td>
&lt;td>66560&lt;/td>
&lt;td>66463&lt;/td>
&lt;td>1024&lt;/td>
&lt;td>0x1000&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>CERTIFICATE&lt;/td>
&lt;td>16512&lt;/td>
&lt;td>16512&lt;/td>
&lt;td>94720&lt;/td>
&lt;td>&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>The fact that it shows the virtual information and offsets should cause one to pause and realize that this is not a .zip file, but an .exe renamed to .zip. Opening this file with another zip utility, such as the Windows default, would have produced an error which would have triggered deeper inspection and caused one to see the MZ file header as well.&lt;/p>
&lt;p>Admittedly, I did not notice the additional columns until I was writing up this document. Since 7zip showed the files, I just suspected that ESET had taken a different approach for this CrackMe and I proceeded to attempt to rebuild the PE manually. You can find that write-up &lt;a href="https://noxwizard.github.io/posts/rebuilding-pe-files/">here&lt;/a>.&lt;/p>
&lt;p>This article will be light on screenshots since they take up a lot of space. Several of the functions also have large graphs that won’t easily fit on the screen. The expectation is for the reader to follow along with their own copy.&lt;/p>
&lt;h2 id="analysis">Analysis&lt;/h2>
&lt;p>The first step is to rename the file to crackme.exe and load it into your reverse engineering tool of choice. For this write-up, I am using IDA Free 7.6. The entrypoint should be identified as &lt;code>0x401CBD&lt;/code>. From there, I just started documenting and renaming functions. My approach for this is mainly following &lt;code>call&lt;/code>s until I reach a Windows API function so I can try to figure out what the function is doing.&lt;/p>
&lt;p>There are a lot of global variables (&lt;code>dword_xxxxxx&lt;/code>), so I tried to identify ones that were obvious and ones that had several cross references pointing to them. If there weren’t many references to it and it wasn’t really clear, I just moved on.&lt;/p>
&lt;p>The first couple of blocks check to see if the binary has a valid DOS, PE, and PE32 header and then checks to see if there is a .NET data directory or not. This mainly seems to be so that it knows whether to call &lt;code>CorExit&lt;/code> or &lt;code>TerminateProcess&lt;/code>.&lt;/p>
&lt;p>The next block (&lt;code>0x401B55&lt;/code>), sets up a global variable (&lt;code>dword_4196E0&lt;/code>) to hold a pointer to the main process heap. It comes from a call to &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/heapapi/nf-heapapi-getprocessheap">GetProcessHeap&lt;/a>.&lt;/p>
&lt;p>The next block (&lt;code>0x401B69&lt;/code>) does a lot of initialization related tasks. &lt;code>sub_402B9F&lt;/code> starts by encoding a NULL pointer and then copying it to several global variables. I never figured out what they were used for, but it didn’t really seem to matter. The rest of the function gets a handle to kernel32.dll via &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getmodulehandlew">GetModuleHandleW&lt;/a> and then uses &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getprocaddress">GetProcAddress&lt;/a> to obtain function pointers to 29 functions and stores their pointers in global variables. These functions are related to Fiber Local Storage, Thread Local Storage, Critical Sections, Semaphores, and a few other functions. It is helpful to rename all of these variables to denote that they are function pointers so that it’s easier to see them during the rest of the analysis. The rest of &lt;code>sub_4027C7&lt;/code> initializes a critical section and sets up local storage.&lt;/p>
&lt;p>It wasn’t immediately obvious what was going on in the next block (&lt;code>0x401B7A&lt;/code>) other than the file’s type was being checked and more critical sections were being initialized.&lt;/p>
&lt;p>The next block (&lt;code>0x401B94&lt;/code>) starts with a call to &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/processenv/nf-processenv-getcommandlinea">GetCommandLineA&lt;/a> and stores its result into the global variable &lt;code>dword_41AEB4&lt;/code>. The block then calls a function (&lt;code>sub_403707&lt;/code>) which fetches the process’ environment variables (via &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/processenv/nf-processenv-getenvironmentstringsw">GetEnvironmentStringsW&lt;/a>) and then converts it from wide characters to multibyte characters. That string is then stored in &lt;code>dword_41906C&lt;/code>. The last function call in this block is to &lt;code>sub_4032D2&lt;/code>. It first makes a call to &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getmodulefilenamea">GetModuleFileNameA&lt;/a> which in this case will retrieve the absolute path of the binary itself. The function then makes calls to &lt;code>sub_403383&lt;/code> which looks like it’s dealing with escapes since it is referencing double quotes, spaces, and slashes. The net effect of this block is that &lt;code>dword_4190A4&lt;/code> contains the path to the binary.&lt;/p>
&lt;p>It is not immediately clear what is going on in the next block (&lt;code>0x401BBA&lt;/code>) other than that it is iterating over the environment variables.&lt;/p>
&lt;p>The next block (&lt;code>0x401BCB&lt;/code>) is where it starts getting interesting.
&lt;img src="../../files/eset-crackme-2017/loc_402AC1.png" alt="Dissassembly @ loc_402ac1">&lt;/p>
&lt;p>&lt;code>sub_40525B&lt;/code> isn’t too interesting, it encodes a pointer to a function which shows an error message and quits. If we look at the next two offsets which are passed into &lt;code>sub_402BF3&lt;/code>, we can observe that they look like addresses. If we convert those into dwords, they become valid references to other functions in the program. The next function, &lt;code>sub_402BF3&lt;/code>, will iterate through the array of functions and call each one. This particular set of functions isn’t too interesting. The first potentially does some stuff related to Code Pages, the second allocates some heap space, encodes its pointer, and stores it in a global variable. The third function isn’t too clear, but it does copy some addresses to another location. The fourth function uses the cpuid instruction to try and determine if it’s running in a VM and sets a global variable. The last function sets up an unhandled exception filter. While these functions aren’t interesting, we see this pattern again a little farther into the same function.&lt;/p>
&lt;p>The second array of functions are from 0x412144 to 0x412154. There are three functions in this array. The first and second get handles to &lt;code>STDIN&lt;/code> and &lt;code>STDOUT&lt;/code> using &lt;a href="https://docs.microsoft.com/en-us/windows/console/getstdhandle">GetStdHandle&lt;/a>. These are stored for later use to read and write to the console. The third function (&lt;code>sub_411390&lt;/code>) is where things get interesting, because it calls &lt;code>sub_401000&lt;/code>.&lt;/p>
&lt;p>The first thing we see in &lt;code>sub_401000&lt;/code>, is a call to &lt;code>sub_4012F0&lt;/code> which simply sets &lt;code>EAX&lt;/code> to &lt;code>fs:30h&lt;/code>. In Windows, the File Segment register (&lt;code>FS&lt;/code>) for 32-bit applications points to the process’ &lt;a href="https://en.wikipedia.org/wiki/Win32_Thread_Information_Block">Thread Environment Block&lt;/a> (&lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/winternl/ns-winternl-teb">MSDN&lt;/a>). At offset 0x30 is the &lt;a href="https://en.wikipedia.org/wiki/Process_Environment_Block">Process Environment Block (PEB)&lt;/a> (&lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/winternl/ns-winternl-peb">MSDN&lt;/a>). The PEB was intended to be an internal data structure only used by the Windows internals. However, its details have slowly been documented over the years. There is also a surprising amount of information about these structures available by using WinDBG.&lt;/p>
&lt;p>The current assembly block we’re looking at contains the following:&lt;/p>
&lt;pre tabindex="0">&lt;code>call get_peb
mov [ebp+peb], eax
mov eax, [ebp+peb]
mov ecx, [eax+0Ch]
mov edx, [ecx+1Ch]
mov [ebp+var_10], edx
&lt;/code>&lt;/pre>&lt;p>From the MSDN documentation, we know that at &lt;code>PEB:0Ch&lt;/code>, is the &lt;code>Ldr&lt;/code> member, which has a type of &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/winternl/ns-winternl-peb_ldr_data">PEB_LDR_DATA&lt;/a>. The MSDN documentation is pretty light for this structure, so let’s look at what WinDBG shows. Running the command &lt;code>dt _PEB_LDR_DATA&lt;/code> in WinDBG yields the following output:&lt;/p>
&lt;pre tabindex="0">&lt;code>0:000&amp;gt; dt _PEB_LDR_DATA
ntdll!_PEB_LDR_DATA
+0x000 Length : Uint4B
+0x004 Initialized : UChar
+0x008 SsHandle : Ptr32 Void
+0x00c InLoadOrderModuleList : _LIST_ENTRY
+0x014 InMemoryOrderModuleList : _LIST_ENTRY
+0x01c InInitializationOrderModuleList : _LIST_ENTRY
+0x024 EntryInProgress : Ptr32 Void
+0x028 ShutdownInProgress : UChar
+0x02c ShutdownThreadId : Ptr32 Void
&lt;/code>&lt;/pre>&lt;p>At offset 0x1C, we see the &lt;code>InInitializationOrderModuleList&lt;/code> member, which has a type of &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/winternl/ns-winternl-peb_ldr_data#remarks">_LIST_ENTRY&lt;/a>. These entries are doubly linked lists which contain a forward entry (&lt;code>FLink&lt;/code>) and backwards entry (&lt;code>BLink&lt;/code>). MSDN tells us that the type of each list entry is &lt;code>LDR_DATA_TABLE_ENTRY&lt;/code>, which contains information about the loaded DLLs for the process.&lt;/p>
&lt;p>So the assembly code now looks like this:&lt;/p>
&lt;pre tabindex="0">&lt;code>call get_peb
mov [ebp+peb], eax
mov eax, [ebp+peb]
mov ecx, [eax+PEB.Ldr]
mov edx, [ecx+PEB_LDR_DATA.InInitializationOrderModuleList.Flink]
mov [ebp+peb_module_list], edx
&lt;/code>&lt;/pre>&lt;p>When trying to traverse the linked lists and line them up with their corresponding data structures, you need to recall that the linked lists are part of the &lt;code>LDR_DATA_TABLE_ENTRY&lt;/code> itself and they aren’t pointers to a linked list. A &lt;code>LIST_ENTRY&lt;/code> works by having the data attached below itself. To access the data within the entry, you typically use the &lt;a href="https://docs.microsoft.com/en-us/windows-hardware/drivers/kernel/mm-bad-pointer#containing_record">CONTAINING_RECORD&lt;/a> macro, pass it the desired type, member field, and its pointer, and it will return a pointer to the data for you.
&lt;img src="../../files/eset-crackme-2017/list_entry.png" alt="List entry">&lt;/p>
&lt;p>Rather than using the &lt;code>CONTAINING_RECORD&lt;/code> macro, you can also manually compute the address by adding the size of &lt;code>LIST_ENTRY&lt;/code> to its pointer.&lt;/p>
&lt;p>In the case of the &lt;code>LDR_DATA_TABLE_ENTRY&lt;/code>, it looks like this:
&lt;img src="../../files/eset-crackme-2017/ldr_module.png" alt="Ldr list entries">&lt;/p>
&lt;p>Since IDA only sees the results of this macro being applied, you need to account for that in your structure definitions. In order for it to be mapped properly, you will need to define a custom &lt;code>LDR_DATA_TABLE_ENTRY&lt;/code> without the &lt;code>InLoadOrderLinks&lt;/code> and &lt;code>InMemoryOrderLinks&lt;/code> fields. This is because our pointer is to the &lt;code>InInitializationOrderLinks&lt;/code> list, so IDA will try to align the structure there instead at the top of the structure (&lt;code>InLoadOrderLinks&lt;/code>).&lt;/p>
&lt;p>The next set of assembly in this function looks like this:&lt;/p>
&lt;pre tabindex="0">&lt;code>loc_40101D:
mov eax, [ebp+peb_module_list]
movzx ecx, word ptr [eax+1Ch]
add ecx, 2
mov [ebp+var_6C], ecx
mov edx, [ebp+var_10]
mov eax, [edx+20h]
mov [ebp+var_64], eax
mov [ebp+var_20], 0
jmp short loc_401045
&lt;/code>&lt;/pre>&lt;p>If you attempted to apply a normal LDR_DATA_TABLE_ENTRY definition to this, you would have ended up with this:&lt;/p>
&lt;pre tabindex="0">&lt;code>loc_40101D:
mov eax, [ebp+peb_module_list]
movzx ecx, word ptr [eax+LDR_MODULE.EntryPoint]
add ecx, 2
mov [ebp+var_6C], ecx
mov edx, [ebp+peb_module_list]
mov eax, [edx+LDR_MODULE.SizeOfImage]
mov [ebp+var_64], eax
mov [ebp+var_20], 0
jmp short loc_401045
&lt;/code>&lt;/pre>&lt;p>When you really wanted this:&lt;/p>
&lt;pre tabindex="0">&lt;code>loc_40101D:
mov eax, [ebp+peb_module_list]
movzx ecx, word ptr [eax+ININITORDER_LDR_MODULE.BaseDllName.Length]
add ecx, 2
mov [ebp+var_6C], ecx
mov edx, [ebp+peb_module_list]
mov eax, [edx+ININITORDER_LDR_MODULE.BaseDllName.Buffer]
mov [ebp+var_64], eax
mov [ebp+var_20], 0
jmp short loc_401045
&lt;/code>&lt;/pre>&lt;p>I didn’t notice this at first and it made the rest of the code very peculiar since I knew it was operating on the DLL name, but thought it was a very strange way to acquire it.&lt;/p>
&lt;p>The next thing that happens is the function iterates through the module list. For each module, it does the following:&lt;/p>
&lt;ol>
&lt;li>Convert the name to all lowercase&lt;/li>
&lt;li>Compute a hash of the function name&lt;/li>
&lt;li>Check that hash against 0x19B9AC28&lt;/li>
&lt;li>If the hash does not match, advance the module list to the next entry, if not already back to the beginning of the list&lt;/li>
&lt;/ol>
&lt;p>If you run the program in a debugger and set a breakpoint here, you will find that it is looking for kernel32.dll.&lt;/p>
&lt;p>The next part of this function uses the base address of kernel32.dll which is loaded into the process memory and parses its PE header. It extracts the RVA to the export directory, the address of the function names and ordinals. Similar to with the module names, the list of exported functions is iterated and the following happens:&lt;/p>
&lt;ol>
&lt;li>The function name is hashed&lt;/li>
&lt;li>The hash is compared against 0xD9A63D0D&lt;/li>
&lt;li>If the hash doesn’t match, a counter is incremented to advance to the next function&lt;/li>
&lt;/ol>
&lt;p>If you run the program in a debugger and set a breakpoint here, you will find that it is looking for &lt;code>GetModuleFileNameW&lt;/code>.&lt;/p>
&lt;p>The counter is then used to find the function’s ordinal using the ordinals list address it saved from earlier. That is then used to find the function’s RVA, which is then added to kernel32’s base address to get a valid function pointer.&lt;/p>
&lt;p>The &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/libloaderapi/nf-libloaderapi-getmodulefilenamew">GetModuleFilenameW&lt;/a> function is then called with a NULL handle to get the full path to the binary. The &lt;code>sub_4018AB&lt;/code> function then trims that full path down to only the name of the binary, without the file extension. &lt;code>sub_401790&lt;/code> then compares the binary name to “&lt;code>Cr4ckM3&lt;/code>”. It doesn’t do this with a traditional string comparison, it is done two bytes at a time and performed as an integer comparison. If you were looking through the binary’s strings, this would not have shown up.&lt;/p>
&lt;p>That wraps up block (&lt;code>0x401BCB&lt;/code>). The next one is &lt;code>0x401BDE&lt;/code>. If your binary does not have the correct name (Cr4ckM3.exe), then it will exit before reaching this block. The first thing &lt;code>sub_4013F0&lt;/code> does is to check and see if a debugger is attached. If you are using IDA, you can set a conditional breakpoint on the &lt;code>test eax, eax&lt;/code> line and set the condition to “&lt;code>EAX = 0&lt;/code>”. This will automatically make the condition succeed so that the program doesn’t exit.&lt;/p>
&lt;p>&lt;code>sub_4013A0&lt;/code> is the next function call we see. This is an xor “encryption” function. It takes 4 arguments: the buffer to en/decrypt, the length of the buffer, the encryption key, and an increment amount. The increment amount is added to each subsequent byte that is en/decrypted. In the first call, we see that buffer &lt;code>0x418000&lt;/code> is being decrypted with the key of 0x25 and an increment amount of 3. The first byte is xor’d with the value of 25, the second byte is xor’d with the value of 28, and so on. The string “Please enter a valid password : “ is printed to the screen and then the buffer is re-encrypted so that it is not as easily spotted in memory.&lt;/p>
&lt;p>The console then waits for input from the user. We can see that &lt;a href="https://docs.microsoft.com/en-us/windows/console/readconsole">ReadConsoleA&lt;/a> has been called with a max length of 10. The next bit of code is checking the supplied password to see if it is correct. A simple string comparison is not being done, so it is not possible to directly obtain the password from the disassembly. We can, however, see what comparisons are being done and try to deduce the password from them.&lt;/p>
&lt;p>The first comparison is:&lt;/p>
&lt;pre tabindex="0">&lt;code>mov edx, 1
imul edx, 7
movsx eax, [ebp+edx+Buffer]
mov ecx, 1
imul ecx, 6
movsx edx, [ebp+ecx+Buffer]
add eax, edx
cmp eax, 0CDh ; 'Í'
&lt;/code>&lt;/pre>&lt;p>Here, letters 7 and 6 are being added together and being compared to 0xCD. For brevity, I won’t show the other five blocks, but their logic is deduced the same way. The final set of comparisons is:&lt;/p>
&lt;table>
&lt;thead>
&lt;tr>
&lt;th>0&lt;/th>
&lt;th>1&lt;/th>
&lt;th>2&lt;/th>
&lt;th>3&lt;/th>
&lt;th>4&lt;/th>
&lt;th>5&lt;/th>
&lt;th>6&lt;/th>
&lt;th>7&lt;/th>
&lt;th>8&lt;/th>
&lt;th>9&lt;/th>
&lt;th>Need&lt;/th>
&lt;/tr>
&lt;/thead>
&lt;tbody>
&lt;tr>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>0xCD&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>0xC9&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>0x13A&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>0x16F&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>&lt;/td>
&lt;td>0xC2&lt;/td>
&lt;/tr>
&lt;tr>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>x&lt;/td>
&lt;td>0x39B&lt;/td>
&lt;/tr>
&lt;/tbody>
&lt;/table>
&lt;p>This results in a system of equations. While we could try to solve it by hand, that’s tedious and we have technology that can do that for us. For this task, we can employ a satisfiability solver, or a SAT solver. A SAT solver will attempt to find a solution that satisfies the constraints which have been provided. In our case, the constraints are the summations across each row.&lt;/p>
&lt;p>There are several SAT solvers available, but for this write-up, I will use the &lt;a href="https://github.com/Z3Prover/z3">Z3 theorem prover&lt;/a> created by Microsoft since it has Python bindings. Below is the script I used:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-python" data-lang="python">&lt;span style="color:#f92672">from&lt;/span> z3 &lt;span style="color:#f92672">import&lt;/span> &lt;span style="color:#f92672">*&lt;/span>
solver &lt;span style="color:#f92672">=&lt;/span> Solver()
L0, L1, L2, L3, L4, L5, L6, L7, L8, L9 &lt;span style="color:#f92672">=&lt;/span> BitVecs(&lt;span style="color:#e6db74">&amp;#39;L0 L1 L2 L3 L4 L5 L6 L7 L8 L9&amp;#39;&lt;/span>, &lt;span style="color:#ae81ff">32&lt;/span>)
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L0, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L0, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L1, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L1, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L2, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L2, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L3, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L3, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L4, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L4, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L5, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L5, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L6, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L6, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L7, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L7, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L8, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L8, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(UGE(L9, &lt;span style="color:#ae81ff">32&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(ULE(L9, &lt;span style="color:#ae81ff">126&lt;/span>))
solver&lt;span style="color:#f92672">.&lt;/span>add(
L6&lt;span style="color:#f92672">+&lt;/span>L7 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">205&lt;/span>,
L5&lt;span style="color:#f92672">+&lt;/span>L8 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">201&lt;/span>,
L3&lt;span style="color:#f92672">+&lt;/span>L6&lt;span style="color:#f92672">+&lt;/span>L7 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">314&lt;/span>,
L4&lt;span style="color:#f92672">+&lt;/span>L5&lt;span style="color:#f92672">+&lt;/span>L8&lt;span style="color:#f92672">+&lt;/span>L9 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">367&lt;/span>,
L0&lt;span style="color:#f92672">+&lt;/span>L1 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">194&lt;/span>,
L0&lt;span style="color:#f92672">+&lt;/span>L1&lt;span style="color:#f92672">+&lt;/span>L2&lt;span style="color:#f92672">+&lt;/span>L3&lt;span style="color:#f92672">+&lt;/span>L4&lt;span style="color:#f92672">+&lt;/span>L5&lt;span style="color:#f92672">+&lt;/span>L6&lt;span style="color:#f92672">+&lt;/span>L7&lt;span style="color:#f92672">+&lt;/span>L8&lt;span style="color:#f92672">+&lt;/span>L9 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">923&lt;/span>
)
print(solver&lt;span style="color:#f92672">.&lt;/span>check())
print(solver&lt;span style="color:#f92672">.&lt;/span>model())
&lt;/code>&lt;/pre>&lt;/div>&lt;p>It sets up bit vectors for each letter of the password and then sets up the various constraints for them. The first 20 constraints are making sure that the values it gives us are within the printable range, since we’ll need to enter this via the keyboard. Normally, z3 allows you to do something like &lt;code>solver.add(L0 &amp;lt; 32)&lt;/code>, but the first time I ran this, it gave me a password of “&lt;code>D~0m~~~OK(&lt;/code>“ which didn’t look correct since that is a lot of repeated values. Z3 was making use of integer overflow to reach some of its answers. So, the initial constraints were changed to use “unsigned greater than or equals” and “unsigned less than or equals” to bound the solutions.&lt;/p>
&lt;p>The new solution provided was: &lt;code>^d0m^iUx`H&lt;/code>&lt;/p>
&lt;p>When this is entered into the program, all checks pass and we reach the following check:&lt;/p>
&lt;pre tabindex="0">&lt;code>mov eax, large fs:30h
movzx eax, byte ptr [eax+2]
test eax, eax
jnz short loc_401632
&lt;/code>&lt;/pre>&lt;p>Recall that &lt;code>fs:30h&lt;/code> holds the PEB. At offset 0x2 into the PEB is the &lt;code>BeingDebugged&lt;/code> field which indicates if a debugger is attached. Earlier there was a call to &lt;code>IsDebuggerPresent()&lt;/code>, which reads the same value. We can defeat this check the same way. Set a conditional breakpoint with a condition of “&lt;code>EAX = 0&lt;/code>”.&lt;/p>
&lt;p>In the next block, we see a call to &lt;a href="https://docs.microsoft.com/en-us/windows/win32/api/sysinfoapi/nf-sysinfoapi-gettickcount">GetTickCount()&lt;/a>. There was one earlier immediately after the call to &lt;code>ReadConsoleA&lt;/code>, which I didn’t mention. The two values are being subtracted to determine how much time has elapsed since the password was entered. This is another way to see if someone is using a debugger to manually step through or modify values. If more than 100 milliseconds have elapsed, the program exits. Set a conditional breakpoint on the jbe line with a condition of “&lt;code>ZF = 1&lt;/code>” to force a jump to the right to continue execution.&lt;/p>
&lt;p>Next, we see a call to &lt;code>sub_401300&lt;/code>, which is computing a hash of the provided password. That hash is then compared to &lt;code>0x1928F914&lt;/code>. In our case, we fail this check and end up taking the right hand side branch which decrypts another string from memory (&lt;code>0x418038&lt;/code>) and then prints it to the screen:&lt;/p>
&lt;pre tabindex="0">&lt;code>Good work. Little help:
char[8] = 85
char[0] + char[2] = 128
char[4] - char[7] = -50
char[6] + char[9] = 219
&lt;/code>&lt;/pre>&lt;p>While we had one correct solution, it was not the only or expected solution. To help us find the right solution, they have provided additional constraints. Now, we just add those constraints into our solver and run it again:&lt;/p>
&lt;div class="highlight">&lt;pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4">&lt;code class="language-python" data-lang="python">solver&lt;span style="color:#f92672">.&lt;/span>add(L8 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">85&lt;/span>)
solver&lt;span style="color:#f92672">.&lt;/span>add(L0 &lt;span style="color:#f92672">+&lt;/span> L2 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">128&lt;/span>)
solver&lt;span style="color:#f92672">.&lt;/span>add(L4 &lt;span style="color:#f92672">-&lt;/span> L7 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#f92672">-&lt;/span>&lt;span style="color:#ae81ff">50&lt;/span>)
solver&lt;span style="color:#f92672">.&lt;/span>add(L6 &lt;span style="color:#f92672">+&lt;/span> L9 &lt;span style="color:#f92672">==&lt;/span> &lt;span style="color:#ae81ff">219&lt;/span>)
&lt;/code>&lt;/pre>&lt;/div>&lt;p>This time, we receive the following password: &lt;code>Pr0m3theUs&lt;/code>
It actually spells something in leetspeak, so it looks promising.&lt;/p>
&lt;p>The password passes all the checks from before and is then used as part of another xor decryption function to decrypt the string at &lt;code>0x4180A8&lt;/code>. It is then printed to the screen:&lt;/p>
&lt;blockquote>
&lt;p>&lt;a href="https://join.eset.com/ae50b61499d27d7da010c718f265a9a1/crackme.zip">https://join.eset.com/ae50b61499d27d7da010c718f265a9a1/crackme.zip&lt;/a>&lt;/p>
&lt;/blockquote>
&lt;p>This is a link to their 2015 crackme. While this crackme has several posted solutions, it does contain a text file with an email address for you to submit your solution.&lt;/p>
&lt;p>For completeness, the other branch with an encrypted string (&lt;code>0x418020&lt;/code>) prints out “Wrong password!”&lt;/p>
&lt;p>I enabled instruction tracing in IDA and ran it with the correct solution and it looks like quite a bit of the program was covered. A lot of the untraced functions looked like error handling, clean up, or were related to locale. Not all of them were, so there might be an easter egg hidden somewhere that I didn’t find.&lt;/p>
&lt;p>That is the end of this write-up. Hopefully it was interesting.&lt;/p></content></item></channel></rss>